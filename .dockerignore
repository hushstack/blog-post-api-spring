# The build context is uploaded to the daemon and anything here can end up in a layer.
# Secrets first: .env carries the R2 keys and the mail password.
.env
.env.*
!.env.example

.git
.gitignore
target/
uploads/
*.iml
.idea/
.vscode/
.settings/
.classpath
.project
HELP.md
Dockerfile
docker-compose.yml
.dockerignore
